# QuickBooks Online MCP server dependencies
# Pin to compatible ranges; bump deliberately after testing.

# MCP server framework (provides mcp.server.fastmcp.FastMCP)
mcp>=1.2.0

# Async HTTP client used for the QBO OAuth2 token exchange and REST calls
httpx>=0.27.0

# Input validation / models (installed transitively by mcp, pinned here explicitly)
pydantic>=2.6.0

# --- Optional -------------------------------------------------------------
# This server talks to the QBO REST API directly (see server.py). The Intuit
# libraries below are NOT imported by server.py, but they are the easiest way to
# perform the one-time OAuth authorization that mints your QBO_REFRESH_TOKEN, and
# they are a reasonable alternative if you later prefer their object model.
# Uncomment to install:
# intuit-oauth>=1.2.6        # imported as `intuitlib` (AuthClient, scopes)
# python-quickbooks>=0.9.12  # imported as `quickbooks` (QuickBooks, objects.*)
